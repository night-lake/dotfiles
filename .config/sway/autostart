# swww: wallpaper
exec_always --no-startup-id wbg /home/amelia/.dotfiles/wallpapers/foggy_peaks.png

# swayosd: volume/brightness indicators
exec_always --no-startup-id swayosd-server

# ironbar: bar
exec_always --no-startup-id waybar

# swaync: notif service
exec_always --no-startup-id swaync

# greenclip: clipboard daemon
exec --no-startup-id greenclip daemon >dev/null

# flameshot: screenshot daemon
exec --no-startup-id flameshot &

# xdg autostart ($XDG_CONFIG_HOME/autostart)
exec --no-startup-id dex --autostart --environment i3

# nm-applet: network systray
exec --no-startup-id nm-applet

# xsslock: screen locking
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# dbus: secret service
exec --no-startup-id dbus-update-activation-environment --all

# pamixer: auto unmute
exec --no-startup-id pamixer -t
