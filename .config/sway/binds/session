# reload the configuration file
bindsym $mod+Shift+c reload

# kill ironbar
bindsym $mod+Shift+r exec --no-startup-id killall -q ironbar

# show power options 
bindsym $mod+p exec --no-startup-id wlogout